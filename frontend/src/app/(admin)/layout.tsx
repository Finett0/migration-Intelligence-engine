import type { ReactNode } from "react";

export const metadata = {
  title: "Admin | Migration Intelligence",
};

export default function AdminGroupLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
